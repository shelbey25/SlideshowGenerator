{
  "name": "slideshow-automation",
  "private": true,
  "version": "1.0.0",
  "description": "Static slideshow generator served locally and on Railway.",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}